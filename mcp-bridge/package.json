{
  "name": "specter-mcp-bridge",
  "version": "1.3.0",
  "description": "MCP server + HTTP bridge for Specter — ghost-cursor browser control for AI agents",
  "type": "module",
  "bin": {
    "specter-mcp": "./bridge.js"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "ws": "^8.21.3"
  }
}
